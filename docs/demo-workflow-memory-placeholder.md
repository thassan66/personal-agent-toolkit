# Demo placeholder: workflow + memory

Replace this file with:

- `demo-workflow-memory.gif`

Recommended recording:

1. run `/workflow capture-note release-checklist`
2. run `/memory`
3. run `/memory-search release`
